const colors = require('colors')
const timeNow = require('./time-now');

colors.setTheme({
	sizeColor: ['strikethrough', 'bold', 'dim'],
	compressedSizeColor: ['bold'],
	arrowColor: ['cyan'],
	fileColor: ['italic']
});

module.exports = function displayDeleted(message) {
	console.log(
		'[' + timeNow().dim + ']' +
		' ' + message.fileColor + ' ' + 'FILE DELETED'.red
	)
}