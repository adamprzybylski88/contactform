const { Transform } = require('stream');
module.exports = () => {
	const streamFunc = new Transform({
		transform(chunk, encoding, callback) {
			this.push(chunk);
			callback();
		}
	});

	return streamFunc;
}

// fs.createReadStream(_src_tree + _file)
// 	.on('end', function(data) {
// 		console.log(`callback ${data}`);
// 	})
// 	.pipe(streamSkip())
// 	// .pipe(compressPdf())
// 	.pipe(
// 		fs.createWriteStream(_dist_tree + _file)
// 	)