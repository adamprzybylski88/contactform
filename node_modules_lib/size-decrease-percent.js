const fs = require('fs')

module.exports = function sizeDecreasePercent(filepath, filepathCompressed) {
	return ((fs.statSync(filepathCompressed).size / fs.statSync(filepath).size * 100) - 100).toFixed(2) + '%';
}
